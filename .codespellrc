# SPDX-License-Identifier: MIT

[codespell]
# List of words to ignore (comma-separated, no spaces)
ignore-words-list = Fron

# broken because superlinter runs with a different workspace prefix
#skip = */*.pdf,*/*.pptx,*.svg
